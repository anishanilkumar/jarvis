{
  "name": "jarvis-panel",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "build:site": "tsc --noEmit && vite build --config vite.public.config.ts",
    "dev:site": "vite --config vite.public.config.ts"
  },
  "dependencies": {
    "@preact/signals": "^2.11.0",
    "preact": "^10.22.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "typescript": "^5.5.0",
    "vite": "^5.3.0"
  }
}
